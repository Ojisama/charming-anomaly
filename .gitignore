node_modules/
dist/
.claude/worktrees/

# worker/: wrangler's local state. `wrangler dev --local` keeps the local D1 here as SQLite (plus
# WAL/shm sidecars and bundle temp dirs) — regenerated by worker/test.sh on every run, never shared.
.wrangler/

# verification screenshots (never commit — they land in the worktree root during browser checks)
/*.png

# subagent-driven-development scratch: per-plan ledger, task briefs, reports and review packages.
# Regenerated from the plan and git history; the plan in docs/ is the artifact worth keeping.
.superpowers/

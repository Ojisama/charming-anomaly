node_modules/
dist/
.claude/worktrees/

# verification screenshots (never commit — they land in the worktree root during browser checks)
/*.png

# generated review sheet (regenerate with `make contact`)
src/reskin/**/_contact.png

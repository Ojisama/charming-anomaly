// STUB — implemented by the ui subagent. Procedural WebAudio SFX, no assets.
// Names used by main.js: shoot, hit, kill, gem, coin, levelup, hurt, death, victory, click, buy

/** Create/resume the AudioContext. Must be called from a user gesture (Play button). */
export function initAudio() {}

export function playSfx(name) {}
